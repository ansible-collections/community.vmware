# Test code for the vmware guest dynamic plugin module
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)

---
- hosts: localhost
  tasks:
    - assert:
        that:
            # TODO: Commenting temporarily once keyed_groups added uncomment following
            # - "{{ groups['debian8_64Guest'] | length }} == 4"
            - hostvars['DC0_H0_VM0'] is defined

