---
- name: Install dependencies
  hosts: localhost
  tasks:
    - pip:
        name: toml

    - name: Install the Python bindings for SELinux
      yum:
        name: libselinux-python
        state: present
